# HOSHUTARO core エンジン — コア依存（軽量）
#
# ML 重依存（OpenVINO 推論ランタイム等）は requirements-ml.txt を参照。
# Track E では本ファイルの依存のみを Tauri sidecar バイナリに同梱し、
# ML 重依存は初回起動時にダウンロードする方針。

# ── Core ──
fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
httpx>=0.27,<1.0
sse-starlette>=2.0,<3.0
python-multipart>=0.0.9,<1.0

# ── Gemini (Built-in LLM) ──
google-generativeai>=0.8

# ── MCP SDK (Plugin Infrastructure) ──
mcp>=1.0

# ── Legacy LLM (Phase 5 で MCP 移行予定) ──
openai>=1.12,<2.0
langgraph>=0.2
langchain-core>=0.3
langchain>=0.3

# ── Data Processing ──
openpyxl>=3.1,<4.0

# ── YAML (Skill definitions) ──
pyyaml>=6.0
